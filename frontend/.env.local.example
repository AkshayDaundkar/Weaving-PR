# Backend API URL. Used for server-side fetches and for Next.js rewrites (so /api/* is proxied here).
# Local dev (default if unset): http://localhost:8000
# Production (e.g. Vercel): set to your deployed backend URL, e.g. https://your-backend.up.railway.app
# NEXT_PUBLIC_API_URL=http://localhost:8000
